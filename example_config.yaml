# This is an example config.
# !!! DO NOT COMMIT REAL CONFIGS TO PUBLIC REPOSITORIES !!!
database:
  type: plug
  host: localhost
  port: 5432
  username: user
  password: pass
  database: logs

settings:
  view: cli #or gui
